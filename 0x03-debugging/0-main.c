#include "main.h"

/**
 * main - a c file to test the output of the function
 * positive_or_negetive
 * Return: always 0 success
 */

int main(void)
{
	int digit = 0;

	positive_or_negative(digit);

	return (0);
}
